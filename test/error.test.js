// Import necessary modules and files
const { handleMessage } = require('../index.js'); // handleMessage function is exported from index.js
const { test } = require('node:test');

test("imageUrl is null") // message.content.startswith('!image') will need to be true, and imageUrl will need to be null

test("No message was generated")

test("Error while calling OpenAI")

test("banError")